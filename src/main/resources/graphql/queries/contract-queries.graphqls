extend type Query {
  # Contract queries
  contracts: [Contract!]!
  contract(id: ID!): Contract
  contractsByStatus(status: ContractStatus!): [Contract!]!
  searchContracts(keyword: String!): [Contract!]!
}