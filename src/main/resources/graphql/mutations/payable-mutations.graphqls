extend type  Mutation {
    # Payable mutations
    createPayable(
        contractId: ID!
        payableInput:PayableInput!
    ): Payable!

    updatePayable(
        id: ID!
        payableInput:PayableInput!
    ): Payable!

    deletePayable(id: ID!): Boolean!
}