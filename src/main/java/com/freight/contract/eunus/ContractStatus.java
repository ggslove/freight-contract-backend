package com.freight.contract.eunus;

public enum ContractStatus {
    // 1. 待签订（草稿 / 待确认）
    // DRAFT,
    // 2. 已生效（执行中）
    ACTIVE,
    // 3.待结算
    PENDING,
    // 4. 已结算（闭环）
    SETTLED,
    //5. 暂停 / 中止
    //SUSPENDED,
    // 6. 终止（解除）
    //TERMINATED;
}
